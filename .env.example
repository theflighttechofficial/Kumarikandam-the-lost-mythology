# This project is a fully static site (Vite + React) with no backend calls
# and no API keys. No environment variables are required to run, build, or
# host it — this file is kept only as a placeholder in case you extend the
# project later.
